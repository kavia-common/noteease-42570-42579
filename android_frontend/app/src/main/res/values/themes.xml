<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use a broadly available AppCompat Light theme without action bar; Compose will render its own top app bars -->
    <style name="Theme.NoteEase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
    </style>
</resources>
